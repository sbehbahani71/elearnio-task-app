import { FC } from 'react';
import { Typography } from '@mui/material';

export const CalculatorPageHeaderComponent: FC = () => {
    return (
        <Typography fontWeight='bold' my={2} variant='h5'>
            Calculator Page
        </Typography>
    );
};
